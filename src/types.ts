export type TestimonialType = {
  id: number;
  name: string;
  role: string;
  content: string;
  avatar: string;
};

export type FeatureType = {
  id: number;
  title: string;
  description: string;
  icon: string;
};

export type FAQType = {
  id: number;
  question: string;
  answer: string;
};